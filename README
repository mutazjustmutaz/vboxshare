This script moves all files and directories from a shared directory to the 
desktop, switches their ownership to the current user, removes files' execution
permissions, and echos all names to the terminal.

